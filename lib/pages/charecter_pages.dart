import 'package:flutter/material.dart';

class CharacterPages extends StatelessWidget {
  const CharacterPages({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Characters'),
      ),
    );
  }
}
